[project]
name = "nanobot-docker"
version = "0.1.5.post1"
description = "docker repo for nanobot"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "nanobot-ai[api,discord]==0.1.5.post1",
]
